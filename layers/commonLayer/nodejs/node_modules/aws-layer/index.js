module.exports = {
    // compatibility layer:
    // UserClient is picked from either UserClient{Dynamo,MySQL} automatically
    // the same thing applies to MerchantClient and LoanClient
    // see pp-dbapi/compat/*
    ...(["prod", "test"].includes(process.env.mode) ? require('pp-dbapi/compat/user')     : require('./dynamo/userTable')),
    ...(["prod", "test"].includes(process.env.mode) ? require('pp-dbapi/compat/loan')     : require('./dynamo/loanTable')),
    ...(["prod", "test"].includes(process.env.mode) ? require('pp-dbapi/compat/merchant') : require('./dynamo/merchantTable')),

    ...require('./dynamo/tokenTable'),
    ...require('./dynamo/eventTable'),

    ...require('./sns'),
    ...require('./lambda'),
    ...require('./s3'),
    ...require('./secretsManager')
};
